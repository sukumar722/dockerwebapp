---
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: devopsapp
  namespace: webapps
spec:
  replicas: 2
  selector:
    matchLabels:
      app: db
  template:
    metadata:
      labels:
        app: db
    spec:
      containers:
        - name: cont-1
          image: sukumar722/k8first:db-image
          ports:
            - containerPort: 3306
      imagePullSecrets:
        - name: flm




---
apiVersion: v1
kind: Service
metadata:
  name: devopsdb
spec:
  clusterIP: None
  selector:
    app: db
  ports:
    - port: 3306
      targetPort: 3306
